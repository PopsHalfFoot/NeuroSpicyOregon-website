NEURO SPICY OREGON — SEO UPGRADE PACKAGE

FILES
- index.html -> website root
- sitemap.xml -> website root
- robots.txt -> website root (new)
- blog/index.html -> blog folder
- blog/style.css -> blog folder (unchanged, included for convenience)
- seven article HTML files -> blog folder

SEO CHANGES
1. Added self-referencing canonical URLs to homepage, blog index, and all seven articles.
2. Added Open Graph and Twitter social-sharing metadata.
3. Added Organization + WebSite JSON-LD to homepage.
4. Added Blog JSON-LD to blog index.
5. Added BlogPosting JSON-LD to all seven articles.
6. Removed obsolete meta keywords tags.
7. Fixed blog/article navigation links that still pointed to old homepage anchor IDs.
8. Added a small number of contextual internal links between related articles.
9. Improved several title tags and meta descriptions for clearer search intent while leaving visible article titles and prose style intact.
10. Cleaned and normalized sitemap.xml.
11. Added robots.txt pointing crawlers to sitemap.xml.

NOTES
- No visual redesign was performed.
- Blog CSS is unchanged.
- The homepage root style.css and script.js were not provided in this batch and do not need changes for this SEO pass.
- Article 1 and Article 2 only contain month-level publication dates in the source HTML, so their JSON-LD intentionally omits datePublished rather than inventing an exact date.
- The shared logo is used as the initial social-share image. A dedicated 1200x630 share image for each article would be a strong later upgrade.
